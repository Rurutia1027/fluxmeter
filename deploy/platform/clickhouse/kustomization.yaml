apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fluxmeter
resources:
  - configmap.yaml
  - statefulset.yaml
  - service.yaml
  - init-cm.yaml
  # Apply after STS Ready (see README) — not in default apply:
  # - init-job.yaml